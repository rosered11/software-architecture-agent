{
  "eval_id": 1,
  "eval_name": "pattern-guidance-bote",
  "prompt": "We have a Go background service processing 2,000 Kafka events per minute. Each event triggers a single PostgreSQL row lookup by ID. p95 latency is spiking to 800ms under load. Connection pool is set to 20. Should I add Redis caching or switch to batch reads?",
  "assertions": [
    {
      "text": "Response includes 'Mode: Pattern Guidance' or 'Pattern Guidance' as a heading",
      "name": "contains-mode-label"
    },
    {
      "text": "Response shows BotE pool math with formula involving query_count x hold_time vs pool size, with actual numbers",
      "name": "contains-bote-pool-math"
    },
    {
      "text": "Response addresses both Redis caching AND batch reads as options",
      "name": "covers-both-options"
    },
    {
      "text": "Response includes a 'when NOT to use' section for at least one option",
      "name": "has-when-not-to-use"
    },
    {
      "text": "Response has a concrete if/then decision rule (not just 'it depends')",
      "name": "has-decision-rule"
    }
  ]
}
